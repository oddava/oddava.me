---
// Old /garden/tag/<tag> links live on as redirects into the notes garden.
// A pure param redirect — no need to enumerate tags from the store.
return Astro.redirect(
  '/notes/tag/' + encodeURIComponent(Astro.params.tag ?? ''),
  301,
);
---
